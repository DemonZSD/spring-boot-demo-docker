<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.weshzhu.dbtool.doc.dao.dao.SchemataDao">

    <resultMap type="org.weshzhu.dbtool.common.entity.Schemata" id="SchemataMap">
        <result property="catalogName" column="CATALOG_NAME" jdbcType="VARCHAR"/>
        <result property="schemaName" column="SCHEMA_NAME" jdbcType="VARCHAR"/>
        <result property="defaultCharacterSetName" column="DEFAULT_CHARACTER_SET_NAME" jdbcType="VARCHAR"/>
        <result property="defaultCollationName" column="DEFAULT_COLLATION_NAME" jdbcType="VARCHAR"/>
        <result property="sqlPath" column="SQL_PATH" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryBySchemaName" resultMap="SchemataMap">
        select
          CATALOG_NAME, SCHEMA_NAME, DEFAULT_CHARACTER_SET_NAME, DEFAULT_COLLATION_NAME, SQL_PATH
        from SCHEMATA
        where  SCHEMA_NAME = #{schemaName}
    </select>



    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from SCHEMATA
        <where>
            <if test="catalogName != null and catalogName != ''">
                and CATALOG_NAME = #{catalogName}
            </if>
            <if test="schemaName != null and schemaName != ''">
                and SCHEMA_NAME = #{schemaName}
            </if>
            <if test="defaultCharacterSetName != null and defaultCharacterSetName != ''">
                and DEFAULT_CHARACTER_SET_NAME = #{defaultCharacterSetName}
            </if>
            <if test="defaultCollationName != null and defaultCollationName != ''">
                and DEFAULT_COLLATION_NAME = #{defaultCollationName}
            </if>
            <if test="sqlPath != null and sqlPath != ''">
                and SQL_PATH = #{sqlPath}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into SCHEMATA(CATALOG_NAME, SCHEMA_NAME, DEFAULT_CHARACTER_SET_NAME, DEFAULT_COLLATION_NAME, SQL_PATH)
        values (#{catalogName}, #{schemaName}, #{defaultCharacterSetName}, #{defaultCollationName}, #{sqlPath})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into SCHEMATA(CATALOG_NAME, SCHEMA_NAME, DEFAULT_CHARACTER_SET_NAME, DEFAULT_COLLATION_NAME, SQL_PATH)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.catalogName}, #{entity.schemaName}, #{entity.defaultCharacterSetName}, #{entity.defaultCollationName}, #{entity.sqlPath})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into SCHEMATA(CATALOG_NAME, SCHEMA_NAME, DEFAULT_CHARACTER_SET_NAME, DEFAULT_COLLATION_NAME, SQL_PATH)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.catalogName}, #{entity.schemaName}, #{entity.defaultCharacterSetName}, #{entity.defaultCollationName}, #{entity.sqlPath})
        </foreach>
        on duplicate key update
        CATALOG_NAME = values(CATALOG_NAME),
        SCHEMA_NAME = values(SCHEMA_NAME),
        DEFAULT_CHARACTER_SET_NAME = values(DEFAULT_CHARACTER_SET_NAME),
        DEFAULT_COLLATION_NAME = values(DEFAULT_COLLATION_NAME),
        SQL_PATH = values(SQL_PATH)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update SCHEMATA
        <set>
            <if test="catalogName != null and catalogName != ''">
                CATALOG_NAME = #{catalogName},
            </if>
            <if test="schemaName != null and schemaName != ''">
                SCHEMA_NAME = #{schemaName},
            </if>
            <if test="defaultCharacterSetName != null and defaultCharacterSetName != ''">
                DEFAULT_CHARACTER_SET_NAME = #{defaultCharacterSetName},
            </if>
            <if test="defaultCollationName != null and defaultCollationName != ''">
                DEFAULT_COLLATION_NAME = #{defaultCollationName},
            </if>
            <if test="sqlPath != null and sqlPath != ''">
                SQL_PATH = #{sqlPath},
            </if>
        </set>
        where  = #{}
    </update>

</mapper>

